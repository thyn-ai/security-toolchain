# The toolchain dogfoods itself. `pip install -e .` first so the `thyn-sec` entry point
# is on PATH for the local hooks below. Third-party hook repositories are pinned to the
# commit their tag pointed at (`pre-commit autoupdate --freeze` keeps that shape); the
# tag stays as a comment.
default_install_hook_types: [pre-commit, pre-push]
repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: 3e8a8703264a2f4a69428a0aa4dcb512790b2c8c # v6.0.0
    hooks:
      - id: check-merge-conflict
      - id: check-added-large-files
        args: ["--maxkb=512"]
      - id: check-yaml
      - id: check-json
      - id: end-of-file-fixer
      - id: trailing-whitespace
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: 2eeb5678de71a00c0902cbda7105d328432f72cb # v0.16.8 (ruff 0.16.8, as requirements-dev.txt)
    hooks:
      - id: ruff
        args: ["--fix"]
      - id: ruff-format
  - repo: local
    hooks:
      - id: gitleaks-staged
        name: gitleaks (staged changes)
        entry: thyn-sec gitleaks-staged
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-commit]
      - id: opengrep-changed
        name: opengrep (changed files)
        entry: thyn-sec opengrep-changed --overlay=python-uv
        language: system
        types_or: [python, yaml, json, toml]
        exclude: ^fixtures/
        stages: [pre-commit]
      - id: actionlint
        name: actionlint
        entry: thyn-sec actionlint
        language: system
        files: ^\.github/workflows/[^/]+\.ya?ml$
        stages: [pre-commit]
      - id: gitleaks-push
        name: gitleaks (commits being pushed)
        entry: thyn-sec gitleaks-push
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-push]
      - id: unit-tests
        name: unit tests
        entry: python -m pytest -q -m "not integration"
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-push]
